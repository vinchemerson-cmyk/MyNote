# 数学公式写法速查

本文整理 Markdown / Obsidian 中常用的 LaTeX 数学公式写法。代码块中的内容可以直接复制。

## 1. 行内公式与独立公式

行内公式用一对 `$` 包围：

```latex
$E=mc^2$
```

效果：$E=mc^2$

独立公式用一对 `$$` 包围：

```latex
$$
E=mc^2
$$
```

效果：

$$
E=mc^2
$$

## 2. 上标、下标与分组

```latex
$x^2$
$a_n$
$x_i^2$
$x^{n+1}$
$a_{i,j}$
```

效果：$x^2$，$a_n$，$x_i^2$，$x^{n+1}$，$a_{i,j}$

超过一个字符时必须用花括号 `{}` 分组：

```latex
$e^{-x}$
$x^{2n+1}$
$a_{n+1}$
```

效果：$e^{-x}$，$x^{2n+1}$，$a_{n+1}$

## 3. 分数、根式与绝对值

```latex
$\frac{a}{b}$
$\dfrac{a}{b}$
$\sqrt{x}$
$\sqrt[n]{x}$
$|x|$
$\lvert x \rvert$
$\|\mathbf{x}\|$
```

效果：$\frac{a}{b}$，$\dfrac{a}{b}$，$\sqrt{x}$，$\sqrt[n]{x}$，$|x|$，$\lvert x \rvert$，$\|\mathbf{x}\|$

复杂分式：

```latex
$$
\frac{x+1}{x^2+2x+1}
$$
```

$$
\frac{x+1}{x^2+2x+1}
$$

## 4. 括号与自动调整大小

普通括号：

```latex
$(a+b)$
$[a,b]$
$\{a,b\}$
```

效果：$(a+b)$，$[a,b]$，$\{a,b\}$

用 `\left` 和 `\right` 让括号自动适应内容高度：

```latex
$$
\left(\frac{a}{b}\right),\quad
\left[\sum_{i=1}^{n}x_i\right],\quad
\left\{\frac{x}{y}\right\}
$$
```

$$
\left(\frac{a}{b}\right),\quad
\left[\sum_{i=1}^{n}x_i\right],\quad
\left\{\frac{x}{y}\right\}
$$

只显示一侧括号时，用英文句点 `.` 代替另一侧：

```latex
$$
\left.\frac{dy}{dx}\right|_{x=0}
$$
```

$$
\left.\frac{dy}{dx}\right|_{x=0}
$$

## 5. 四则运算与关系符号

```latex
$a+b$
$a-b$
$a\times b$
$a\cdot b$
$a\div b$
$a=b$
$a\ne b$
$a<b$
$a>b$
$a\le b$
$a\ge b$
$a\approx b$
$a\equiv b$
$a\propto b$
```

效果：$a+b$，$a-b$，$a\times b$，$a\cdot b$，$a\div b$，$a=b$，$a\ne b$，$a<b$，$a>b$，$a\le b$，$a\ge b$，$a\approx b$，$a\equiv b$，$a\propto b$

## 6. 常用希腊字母

```latex
$\alpha$ $\beta$ $\gamma$ $\delta$ $\epsilon$ $\varepsilon$
$\theta$ $\vartheta$ $\lambda$ $\mu$ $\nu$ $\xi$
$\pi$ $\rho$ $\sigma$ $\tau$ $\phi$ $\varphi$
$\chi$ $\psi$ $\omega$
```

效果：$\alpha$ $\beta$ $\gamma$ $\delta$ $\epsilon$ $\varepsilon$ $\theta$ $\vartheta$ $\lambda$ $\mu$ $\nu$ $\xi$ $\pi$ $\rho$ $\sigma$ $\tau$ $\phi$ $\varphi$ $\chi$ $\psi$ $\omega$

大写希腊字母：

```latex
$\Gamma$ $\Delta$ $\Theta$ $\Lambda$ $\Xi$ $\Pi$
$\Sigma$ $\Phi$ $\Psi$ $\Omega$
```

效果：$\Gamma$ $\Delta$ $\Theta$ $\Lambda$ $\Xi$ $\Pi$ $\Sigma$ $\Phi$ $\Psi$ $\Omega$

## 7. 常用数学符号

```latex
$\infty$          % 无穷
$\partial$        % 偏微分符号
$\nabla$          % 梯度算子
$\pm$             % 正负
$\mp$             % 负正
$\therefore$      % 所以
$\because$        % 因为
$\forall$         % 任意
$\exists$         % 存在
$\neg$            % 非
$\land$           % 且
$\lor$            % 或
$\rightarrow$     % 右箭头
$\Rightarrow$     % 推出
$\Leftrightarrow$ % 等价
```

效果：$\infty$，$\partial$，$\nabla$，$\pm$，$\mp$，$\therefore$，$\because$，$\forall$，$\exists$，$\neg$，$\land$，$\lor$，$\rightarrow$，$\Rightarrow$，$\Leftrightarrow$

> `%` 后面的内容是 LaTeX 注释，不会显示在公式中。

## 8. 求和、连乘、极限与积分

### 求和与连乘

```latex
$$
\sum_{i=1}^{n}x_i
$$
```

$$
\sum_{i=1}^{n}x_i
$$

```latex
$$
\prod_{k=1}^{n}a_k
$$
```

$$
\prod_{k=1}^{n}a_k
$$

### 极限

```latex
$$
\lim_{x\to 0}\frac{\sin x}{x}=1
$$
```

$$
\lim_{x\to 0}\frac{\sin x}{x}=1
$$

### 积分

```latex
$$
\int_a^b f(x)\,dx
$$
```

$$
\int_a^b f(x)\,dx
$$

```latex
$$
\iint_D f(x,y)\,dA
$$
```

$$
\iint_D f(x,y)\,dA
$$

```latex
$$
\iiint_V f(x,y,z)\,dV
$$
```

$$
\iiint_V f(x,y,z)\,dV
$$

```latex
$$
\oint_C \mathbf{F}\cdot d\mathbf{r}
$$
```

$$
\oint_C \mathbf{F}\cdot d\mathbf{r}
$$

## 9. 导数与偏导数

```latex
$f'(x)$
$f''(x)$
$\frac{dy}{dx}$
$\frac{d^2y}{dx^2}$
$\frac{\partial f}{\partial x}$
$\frac{\partial^2 f}{\partial x^2}$
$\frac{\partial^2 f}{\partial x\partial y}$
```

效果：$f'(x)$，$f''(x)$，$\frac{dy}{dx}$，$\frac{d^2y}{dx^2}$，$\frac{\partial f}{\partial x}$，$\frac{\partial^2 f}{\partial x^2}$，$\frac{\partial^2 f}{\partial x\partial y}$

梯度、散度与旋度：

```latex
$$
\nabla f,qquad
\nabla\cdot\mathbf{F},qquad
\nabla\times\mathbf{F}
$$
```

$$
\nabla f,qquad
\nabla\cdot\mathbf{F},qquad
\nabla\times\mathbf{F}
$$

## 10. 常用函数

函数名建议使用 LaTeX 命令，使其保持正体：

```latex
$\sin x$
$\cos x$
$\tan x$
$\arcsin x$
$\ln x$
$\log_a x$
$\exp(x)$
$\max(x,y)$
$\min(x,y)$
```

效果：$\sin x$，$\cos x$，$\tan x$，$\arcsin x$，$\ln x$，$\log_a x$，$\exp(x)$，$\max(x,y)$，$\min(x,y)$

## 11. 集合

```latex
$x\in A$
$x\notin A$
$A\subset B$
$A\subseteq B$
$A\supseteq B$
$A\cup B$
$A\cap B$
$A\setminus B$
$\varnothing$
$\mathbb{N}$
$\mathbb{Z}$
$\mathbb{Q}$
$\mathbb{R}$
$\mathbb{C}$
```

效果：$x\in A$，$x\notin A$，$A\subset B$，$A\subseteq B$，$A\supseteq B$，$A\cup B$，$A\cap B$，$A\setminus B$，$\varnothing$，$\mathbb{N}$，$\mathbb{Z}$，$\mathbb{Q}$，$\mathbb{R}$，$\mathbb{C}$

集合定义：

```latex
$$
A=\left\{x\in\mathbb{R}\mid x>0\right\}
$$
```

$$
A=\left\{x\in\mathbb{R}\mid x>0\right\}
$$

## 12. 向量、矩阵与行列式

### 向量

```latex
$\mathbf{x}$
$\boldsymbol{\alpha}$
$\vec{x}$
$\hat{x}$
$\bar{x}$
$\dot{x}$
$\ddot{x}$
```

效果：$\mathbf{x}$，$\boldsymbol{\alpha}$，$\vec{x}$，$\hat{x}$，$\bar{x}$，$\dot{x}$，$\ddot{x}$

列向量：

```latex
$$
\mathbf{x}=
\begin{bmatrix}
x_1\\
x_2\\
x_3
\end{bmatrix}
$$
```

$$
\mathbf{x}=
\begin{bmatrix}
x_1\\
x_2\\
x_3
\end{bmatrix}
$$

### 矩阵

```latex
$$
A=
\begin{bmatrix}
a_{11} & a_{12}\\
a_{21} & a_{22}
\end{bmatrix}
$$
```

$$
A=
\begin{bmatrix}
a_{11} & a_{12}\\
a_{21} & a_{22}
\end{bmatrix}
$$

矩阵中 `&` 用来分列，`\\` 用来换行。常见矩阵括号：

```latex
\begin{matrix}  ... \end{matrix}   % 无括号
\begin{pmatrix} ... \end{pmatrix}  % 圆括号
\begin{bmatrix} ... \end{bmatrix}  % 方括号
\begin{Bmatrix} ... \end{Bmatrix}  % 花括号
\begin{vmatrix} ... \end{vmatrix}  % 行列式
```

### 行列式、转置与逆矩阵

```latex
$$
\det(A)=
\begin{vmatrix}
a & b\\
c & d
\end{vmatrix}
=ad-bc
$$
```

$$
\det(A)=
\begin{vmatrix}
a & b\\
c & d
\end{vmatrix}
=ad-bc
$$

```latex
$A^{\mathsf T}$
$A^{-1}$
$\operatorname{tr}(A)$
$\operatorname{rank}(A)$
```

效果：$A^{\mathsf T}$，$A^{-1}$，$\operatorname{tr}(A)$，$\operatorname{rank}(A)$

## 13. 分段函数与方程组

### 分段函数

```latex
$$
f(x)=
\begin{cases}
x^2, & x\ge 0,\\
-x,  & x<0.
\end{cases}
$$
```

$$
f(x)=
\begin{cases}
x^2, & x\ge 0,\\
-x,  & x<0.
\end{cases}
$$

### 方程组

```latex
$$
\begin{cases}
2x+y=5,\\
x-y=1.
\end{cases}
$$
```

$$
\begin{cases}
2x+y=5,\\
x-y=1.
\end{cases}
$$

## 14. 多行公式与对齐

使用 `aligned`，在需要对齐的位置放 `&`，每行末尾用 `\\`：

```latex
$$
\begin{aligned}
(a+b)^2
&=(a+b)(a+b)\\
&=a^2+2ab+b^2
\end{aligned}
$$
```

$$
\begin{aligned}
(a+b)^2
&=(a+b)(a+b)\\
&=a^2+2ab+b^2
\end{aligned}
$$

多个等式对齐：

```latex
$$
\begin{aligned}
x+y &= 10,\\
x-y &= 2,\\
2x  &= 12,\\
x   &= 6.
\end{aligned}
$$
```

$$
\begin{aligned}
x+y &= 10,\\
x-y &= 2,\\
2x  &= 12,\\
x   &= 6.
\end{aligned}
$$

## 15. 字体与文字说明

```latex
$\mathrm{d}x$           % 正体
$\mathit{ABC}$          % 斜体
$\mathbf{x}$            % 粗体
$\mathsf{ABC}$          % 无衬线体
$\mathcal{L}$           % 花体
$\mathbb{R}$            % 黑板粗体
$\text{当 }x>0\text{ 时}$ % 公式内插入普通文字
```

效果：$\mathrm{d}x$，$\mathit{ABC}$，$\mathbf{x}$，$\mathsf{ABC}$，$\mathcal{L}$，$\mathbb{R}$，$\text{当 }x>0\text{ 时}$

自定义函数名：

```latex
$\operatorname{sgn}(x)$
$\operatorname{diag}(a_1,\ldots,a_n)$
```

效果：$\operatorname{sgn}(x)$，$\operatorname{diag}(a_1,\ldots,a_n)$

## 16. 空格、省略号与标注

### 空格

```latex
$a\,b$      % 小空格
$a\:b$      % 中等空格
$a\;b$      % 大空格
$a\quad b$  % 更大空格
$a\qquad b$ % 最大空格
```

### 省略号

```latex
$x_1,x_2,\ldots,x_n$
$1+2+\cdots+n$
$\vdots$
$\ddots$
```

效果：$x_1,x_2,\ldots,x_n$，$1+2+\cdots+n$，$\vdots$，$\ddots$

### 上下标注

```latex
$\overline{AB}$
$\underline{x}$
$\overbrace{a+b+c}^{3\text{ 项}}$
$\underbrace{x+\cdots+x}_{n\text{ 个 }x}$
```

效果：$\overline{AB}$，$\underline{x}$，$\overbrace{a+b+c}^{3\text{ 项}}$，$\underbrace{x+\cdots+x}_{n\text{ 个 }x}$

## 17. 概率与统计

```latex
$P(A)$
$P(A\mid B)$
$A\perp B$
$X\sim\mathcal{N}(\mu,\sigma^2)$
$\mathbb{E}[X]$
$\operatorname{Var}(X)$
$\operatorname{Cov}(X,Y)$
$\bar{x}=\frac{1}{n}\sum_{i=1}^{n}x_i$
```

效果：$P(A)$，$P(A\mid B)$，$A\perp B$，$X\sim\mathcal{N}(\mu,\sigma^2)$，$\mathbb{E}[X]$，$\operatorname{Var}(X)$，$\operatorname{Cov}(X,Y)$，$\bar{x}=\frac{1}{n}\sum_{i=1}^{n}x_i$

正态分布密度函数：

```latex
$$
f(x)=\frac{1}{\sigma\sqrt{2\pi}}
\exp\left[-\frac{(x-\mu)^2}{2\sigma^2}\right]
$$
```

$$
f(x)=\frac{1}{\sigma\sqrt{2\pi}}
\exp\left[-\frac{(x-\mu)^2}{2\sigma^2}\right]
$$

## 18. 动态系统常用公式

### 状态空间方程

```latex
$$
\begin{aligned}
\dot{\mathbf{x}}(t) &= A\mathbf{x}(t)+B\mathbf{u}(t),\\
\mathbf{y}(t) &= C\mathbf{x}(t)+D\mathbf{u}(t).
\end{aligned}
$$
```

$$
\begin{aligned}
\dot{\mathbf{x}}(t) &= A\mathbf{x}(t)+B\mathbf{u}(t),\\
\mathbf{y}(t) &= C\mathbf{x}(t)+D\mathbf{u}(t).
\end{aligned}
$$

### 传递函数

```latex
$$
G(s)=\frac{Y(s)}{U(s)}
=C(sI-A)^{-1}B+D
$$
```

$$
G(s)=\frac{Y(s)}{U(s)}
=C(sI-A)^{-1}B+D
$$

### 特征方程

```latex
$$
\det(\lambda I-A)=0
$$
```

$$
\det(\lambda I-A)=0
$$

### 拉普拉斯变换

```latex
$$
\mathcal{L}\{f(t)\}
=F(s)
=\int_0^{\infty}f(t)e^{-st}\,dt
$$
```

$$
\mathcal{L}\{f(t)\}
=F(s)
=\int_0^{\infty}f(t)e^{-st}\,dt
$$

## 19. 常见错误

### 多字符上下标没有分组

错误：

```latex
$x^n+1$
```

这表示 $x^n+1$，而不是整个 `n+1` 都在上标中。

正确：

```latex
$x^{n+1}$
```

### 花括号想显示却没有转义

错误：

```latex
${a,b}$
```

正确：

```latex
$\{a,b\}$
```

### 在公式中直接输入中文

不推荐：

```latex
$x>0 时$
```

推荐：

```latex
$x>0\text{ 时}$
```

### 命令后紧跟字母

LaTeX 会把连续字母当作命令名。需要用空格或花括号断开：

```latex
$\alpha x$
$\alpha{}x$
```

## 20. 可直接复制的公式模板

### 一元二次方程求根公式

```latex
$$
x=\frac{-b\pm\sqrt{b^2-4ac}}{2a}
$$
```

### 二项式定理

```latex
$$
(a+b)^n=\sum_{k=0}^{n}\binom{n}{k}a^{n-k}b^k
$$
```

### 欧拉公式

```latex
$$
e^{i\theta}=\cos\theta+i\sin\theta
$$
```

### 泰勒展开

```latex
$$
f(x)=\sum_{n=0}^{\infty}
\frac{f^{(n)}(a)}{n!}(x-a)^n
$$
```

### 傅里叶变换

```latex
$$
F(\omega)=\int_{-\infty}^{\infty}
f(t)e^{-i\omega t}\,dt
$$
```

### 常微分方程

```latex
$$
a_n\frac{d^ny}{dt^n}
+a_{n-1}\frac{d^{n-1}y}{dt^{n-1}}
+\cdots+a_1\frac{dy}{dt}+a_0y=f(t)
$$
```

### 线性方程组的矩阵形式

```latex
$$
A\mathbf{x}=\mathbf{b}
$$
```

### 矩阵乘法

```latex
$$
(AB)_{ij}=\sum_{k=1}^{n}a_{ik}b_{kj}
$$
```

### 约束优化

```latex
$$
\begin{aligned}
\min_{\mathbf{x}}\quad & f(\mathbf{x})\\
\text{s.t.}\quad
& g_i(\mathbf{x})\le 0,\quad i=1,\ldots,m,\\
& h_j(\mathbf{x})=0,\quad j=1,\ldots,p.
\end{aligned}
$$
```

## 21. 最短记忆清单

```latex
$...$                         % 行内公式
$$...$$                       % 独立公式
x^{上标}                      % 上标
x_{下标}                      % 下标
\frac{分子}{分母}             % 分数
\sqrt[n]{内容}                % n 次根
\sum_{下限}^{上限}            % 求和
\int_{下限}^{上限}            % 积分
\begin{bmatrix}...\end{bmatrix} % 矩阵
\begin{cases}...\end{cases}     % 分段函数或方程组
\begin{aligned}...\end{aligned} % 多行对齐
\text{普通文字}               % 公式中的文字
```

写公式时，先确定结构，再逐层填写内容：

```latex
$$
\text{外层结构}
\left(
  \frac{\text{分子}}{\text{分母}}
\right)
$$
```
