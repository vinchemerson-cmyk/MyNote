---
tags:
  - CAN
  - freeRTOS
---
# freeRTOS
今天完成了最基础的freertos的应用,
```C
/* USER CODE BEGIN Header */  
/**  
  ******************************************************************************  * File Name          : freertos.c  * Description        : Code for freertos applications  ******************************************************************************  * @attention  *  * Copyright (c) 2026 STMicroelectronics.  * All rights reserved.  *  * This software is licensed under terms that can be found in the LICENSE file  * in the root directory of this software component.  * If no LICENSE file comes with this software, it is provided AS-IS.  *  ******************************************************************************  *//* USER CODE END Header */  
  
/* Includes ------------------------------------------------------------------*/  
#include "FreeRTOS.h"  
#include "task.h"  
#include "main.h"  
#include "cmsis_os.h"  
  
/* Private includes ----------------------------------------------------------*/  
/* USER CODE BEGIN Includes */  
#include "uart.h"             /* UART头文件：导出 huart1 句柄和 MX_USART1_UART_Init() */#include "can.h"              /* CAN头文件：导出 hcan 句柄和 MX_CAN_Init/Start() *//* USER CODE END Includes */  
  
/* Private typedef -----------------------------------------------------------*/  
/* USER CODE BEGIN PTD */  
  
/* USER CODE END PTD */  
  
/* Private define ------------------------------------------------------------*/  
/* USER CODE BEGIN PD */  
  
/* USER CODE END PD */  
  
/* Private macro -------------------------------------------------------------*/  
/* USER CODE BEGIN PM */  
  
/* USER CODE END PM */  
  
/* Private variables ---------------------------------------------------------*/  
/* USER CODE BEGIN Variables */  
osSemaphoreId_t binarySemHandle;  /* 二进制信号量句柄：用于 LEDTask → SerialTask 同步 *//* USER CODE END Variables */  
/* Definitions for LEDTask */  
osThreadId_t LEDTaskHandle;  
const osThreadAttr_t LEDTask_attributes = {  
  .name = "LEDTask",  
  .stack_size = 128 * 4,  
  .priority = (osPriority_t) osPriorityNormal,  
};  
/* Definitions for BtnQueue */  
osMessageQueueId_t BtnQueueHandle;  
const osMessageQueueAttr_t BtnQueue_attributes = {  
  .name = "BtnQueue"  
};  
  
/* Private function prototypes -----------------------------------------------*/  
/* USER CODE BEGIN FunctionPrototypes */  
  
/* USER CODE END FunctionPrototypes */  
  
void StartDefaultTask(void *argument);  
  
void MX_FREERTOS_Init(void); /* (MISRA C 2004 rule 8.1) */  
  
/**  
  * @brief  FreeRTOS initialization  * @param  None  * @retval None  */
 void MX_FREERTOS_Init(void) {  
  /* USER CODE BEGIN Init */  
  
  /* USER CODE END Init */  
  /* USER CODE BEGIN RTOS_MUTEX */  /* add mutexes, ... */   /* 在此处添加互斥量（保护共享资源，如UART/CAN的并发访问） */  /* USER CODE END RTOS_MUTEX */  
  /* USER CODE BEGIN RTOS_SEMAPHORES */  /* 创建二进制信号量：max=1（二进制），initial=0（初始不可用，SerialTask启动即阻塞） */  
  binarySemHandle = osSemaphoreNew(1, 0, NULL);  
  /* USER CODE END RTOS_SEMAPHORES */  
  
  /* USER CODE BEGIN RTOS_TIMERS */  /* start timers, add new ones, ... */  /* 在此处添加软件定时器 */  /* USER CODE END RTOS_TIMERS */  
  /* Create the queue(s) */  /* creation of BtnQueue */  
  BtnQueueHandle = osMessageQueueNew (16, sizeof(uint16_t), &BtnQueue_attributes);  
  
  /* USER CODE BEGIN RTOS_QUEUES */  
  /* add queues, ... */   /* 在此处添加消息队列（用于任务间数据传递） */  /* USER CODE END RTOS_QUEUES */  
  /* Create the thread(s) */  /* creation of LEDTask */  
  LEDTaskHandle = osThreadNew(StartDefaultTask, NULL, &LEDTask_attributes);  
  
  /* USER CODE BEGIN RTOS_THREADS */  
  /* 创建串口发送任务：等待二进制信号量 → 发送JustFloat帧 */  
  SerialTaskHandle = osThreadNew(StartSerialTask, NULL, &SerialTask_attributes);  
 /* 创建CAN发送任务：每500ms发送一帧CAN数据（ID=0x123，计数器递增） */  
 CAN_TxTaskHandle = osThreadNew(StartCAN_TxTask, NULL, &CAN_TxTask_attributes);  
  /* USER CODE END RTOS_THREADS */  
  
  /* USER CODE BEGIN RTOS_EVENTS */  /* add events, ... */   /* 在此处添加事件标志组 */  /* USER CODE END RTOS_EVENTS */  
}  
  
/* USER CODE BEGIN Header_StartDefaultTask */  
/**  
  * @brief  Function implementing the LEDTask thread.  *         LED任务：负责切换板载LED并通知串口任务发送数据  
  * @param  argument: Not used  未使用，保留给CMSIS-RTOS兼容  
  * @retval None  *//* USER CODE END Header_StartDefaultTask */  
void StartDefaultTask(void *argument)  
{  
  /* USER CODE BEGIN StartDefaultTask */  
  /* Infinite loop */    /* FreeRTOS任务必须是一个永不返回的死循环 */ 
  for(;;)                /* 无限循环：任务被调度时反复执行 */  
  {  
    HAL_GPIO_TogglePin(GPIOC, GPIO_PIN_13);  /* 翻转PC13引脚电平 → 板载LED亮/灭切换 */    osSemaphoreRelease(binarySemHandle);      /* 释放二进制信号量 → SerialTask解除阻塞并发送串口帧 */    osDelay(200);         /* 阻塞延时200ms（任务进入Blocked状态，让出CPU给其他任务） */  
    }  
  /* USER CODE END StartDefaultTask */  
}  
  
/* Private application code --------------------------------------------------*/  
/* USER CODE BEGIN Application */  
  
/* USER CODE END Application */
```